import designSystemOptions from 'styles/designSystemOptions'
import styled from 'styled-components'

export const Wrapper = styled.ul`
  list-style: none;
  display: flex;
  gap: ${designSystemOptions.spacing.components.medium};
`
